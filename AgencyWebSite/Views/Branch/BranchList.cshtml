@model List<AgencyWebSite.Entities.Branch>
@{
    ViewBag.Title = "BranchList";
    Layout = "~/Views/AdminLayout/Index.cshtml";
    var count = 0;
}


<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Branşlar Sayfası</h4>

            <p class="card-description">
                Branşlar ile ilgili işlemleri buradan yapabilirsiniz
            </p>

            <div class="table-responsive">
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>No</th>
                            <th>Branş Adı</th>
                            <th>Sil</th>
                            <th>Güncelle</th>
                        </tr>
                    </thead>

                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@count</td>
                                <td>@item.BranchName</td>
                                <td><a class="btn btn-danger" href="/Branch/DeleteBranch/@item.BranchId"><i class="fa fa-trash"></i>Sil</a></td>
                                <td><a class="btn btn-success" href="/Branch/UpdateBranch/@item.BranchId"><i class="fa fa-pencil">Güncelle</i></a></td>
                            </tr>
                            count++;
                        }
                    </tbody>
                </table>
                <br />
                <a href="/Branch/CreateBranch" class="btn btn-info"><i class="mdi mdi-file-check btn-icon-append">Branş Oluştur</i></a>
            </div>
        </div>
    </div>
</div>
